Задание по работе с таблицами.

Комментарий к заданию:

На основании предоставленных данных я создала три таблицы.

Таблица с заказами будет содержать только ту информацию, которая касается непосредственно 
заказа, она не будет содержать данных о билетах. С помощью уникального id заказа мы 
попадем в таблицу, которая хранит данные о наших билетах. Одна строка содержит 
информацию об одном билете. ticket_price содержит информацию о стандартной цене билета, 
а ticket_price_after_discount - информацию о цене билета после скидки.

По ticket_type_id мы попадем в таблицу с типами билетов, в которой хранятся занные о 
скидке. type_name - содержит название типа льготного билета, а discount - размер 
скидки (возможно, в процентах).

